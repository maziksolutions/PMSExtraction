[phases.setup]
nixPkgs = ["python311", "libpq"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["alembic upgrade head || true"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
